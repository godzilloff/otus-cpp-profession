name: 'C++ CI'

on:
  push:
    tags:
      - v*
    branches:
      - 'hw4'

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
        with:
          submodules: true
      
      # Установка только необходимых зависимостей
      - name: Install minimal dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y --no-install-recommends \
            cmake \
            build-essential \
            doxygen \
            graphviz
      
      - name: Configure CMake
        working-directory: ./hw4/print_ip
        run: cmake -DCMAKE_BUILD_TYPE=Release -DPATCH_VERSION=${{ github.run_number }} .
      
      - name: Build
        working-directory: ./hw4/print_ip
        run: cmake --build . --parallel  # Параллельная сборка
      
      - name: Run tests
        working-directory: ./hw4/print_ip
        run: ctest --output-on-failure --parallel 4  # Параллельные тесты
      
      - name: Package
        working-directory: ./hw4/print_ip
        run: cmake --build . --target package --parallel
      
      - name: Create Release and Upload Asset
        run: |
          FILE_PATH="./hw4/print_ip/print_ip-0.4.${{ github.run_number }}-Linux.deb"
          [ ! -f "$FILE_PATH" ] && { echo "Error: File $FILE_PATH not found!"; exit 1; }
          gh release create ${{ github.ref_name }} \
            --title "Release ${{ github.ref_name }}" \
            --draft=false \
            --prerelease=false \
            --notes "" \
            "$FILE_PATH"
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Generate Doxygen HTML
        run: |
          cd hw4/print_ip
          mkdir -p docs/html
          sed -i 's|OUTPUT_DIRECTORY\s*=\s*.*|OUTPUT_DIRECTORY = ./docs|' Doxyfile
          sed -i 's|HTML_OUTPUT\s*=\s*.*|HTML_OUTPUT = html|' Doxyfile
          doxygen Doxyfile

      - name: Deploy to GitHub Pages
        uses: peaceiris/actions-gh-pages@v3
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./hw4/print_ip/docs/html
          destination_dir: hw4
          keep_files: true
